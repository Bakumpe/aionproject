'use strict';

/**
 * relocation-request service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::relocation-request.relocation-request');
